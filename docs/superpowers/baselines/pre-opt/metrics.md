# Pre-optimization baseline — `test_10x` (manual0)

Fixed "before" reference for the Phase 4 memory optimization. Generated by
`python -m profiling.cli "<test_10x>" --out profile_out --region manual0 --perpair`
on the unoptimized code (branch point: `main` @ 043845e), 3 runs.

## Peak / mean RSS (3 runs)

| run | peak RSS (MB) | mean RSS (MB) |
|-----|---------------|---------------|
| 1   | 3993          | 2231          |
| 2   | 3961          | 2192          |
| 3   | 3960          | 2251          |
| **avg** | **≈ 3971** | **≈ 2225** |

Peak RSS is stable across runs (~3.97 GB), so % improvement after optimization
will be measured against **peak ≈ 3971 MB** and **mean ≈ 2225 MB**.

## Attribution (Phase 1)

Top integrated-memory contributors (`functions.csv`):
- `core:_fuse_tiles_chunked_plane` ≈ 61%
- `numpy zeros_like` ≈ 35%
- → top 2 ≈ 96% of integrated memory.

## Registration (Phase 2)

43 pairs, raster scan, duration CV ≈ 0.06–0.08 (uniform — no per-pair
inefficiency; the memory cost is in Fuse).

## Archived artifacts (this folder)

`timeline.csv`, `functions.csv`, `pairs.csv` and the figures
`timeline.png`, `functions.png`, `pareto.png`, `swimlanes.png`.
These are the "Before" assets for the Notion page and the Phase 5 overlay.
