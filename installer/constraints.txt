# Pinned build dependencies for the desktop binaries (macOS / Linux / Windows).
#
# These are the EXACT versions that resolved in the green Python 3.12 build of commit
# 32844ce, captured from each platform's `pip freeze` and verified IDENTICAL across all
# three OSes. Pinning here makes releases reproducible and guarantees the three artifacts
# ship the same dependency versions (an unpinned ">=" install can drift between rebuilds).
#
# To refresh: bump Python/deps, let the builds run (they print "=== RESOLVED VERSIONS ==="
# via pip freeze), confirm the three platforms still agree, and update these pins to the
# new concrete versions. Do NOT hand-edit to guessed versions.
#
# Build toolchain: Python 3.12.

numpy==2.4.6
scipy==1.18.0
numba==0.65.1
llvmlite==0.47.0
tensorstore==0.1.84
tifffile==2026.6.1
pandas==3.0.3
scikit-image==0.26.0
tqdm==4.68.3
psutil==7.2.2
threadpoolctl==3.6.0
imageio==2.37.3
matplotlib==3.11.0
PyQt5==5.15.11
napari==0.7.1
napari-ome-zarr==0.8.2
ome-zarr==0.18.0
dask==2026.6.0
pyinstaller==6.21.0
